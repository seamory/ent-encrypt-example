// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"main/ent/accountinfo"
	"main/ent/internal"
	"main/ent/predicate"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// AccountInfoQuery is the builder for querying AccountInfo entities.
type AccountInfoQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.AccountInfo
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the AccountInfoQuery builder.
func (aiq *AccountInfoQuery) Where(ps ...predicate.AccountInfo) *AccountInfoQuery {
	aiq.predicates = append(aiq.predicates, ps...)
	return aiq
}

// Limit adds a limit step to the query.
func (aiq *AccountInfoQuery) Limit(limit int) *AccountInfoQuery {
	aiq.limit = &limit
	return aiq
}

// Offset adds an offset step to the query.
func (aiq *AccountInfoQuery) Offset(offset int) *AccountInfoQuery {
	aiq.offset = &offset
	return aiq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (aiq *AccountInfoQuery) Unique(unique bool) *AccountInfoQuery {
	aiq.unique = &unique
	return aiq
}

// Order adds an order step to the query.
func (aiq *AccountInfoQuery) Order(o ...OrderFunc) *AccountInfoQuery {
	aiq.order = append(aiq.order, o...)
	return aiq
}

// First returns the first AccountInfo entity from the query.
// Returns a *NotFoundError when no AccountInfo was found.
func (aiq *AccountInfoQuery) First(ctx context.Context) (*AccountInfo, error) {
	nodes, err := aiq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{accountinfo.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (aiq *AccountInfoQuery) FirstX(ctx context.Context) *AccountInfo {
	node, err := aiq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first AccountInfo ID from the query.
// Returns a *NotFoundError when no AccountInfo ID was found.
func (aiq *AccountInfoQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = aiq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{accountinfo.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (aiq *AccountInfoQuery) FirstIDX(ctx context.Context) int {
	id, err := aiq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single AccountInfo entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one AccountInfo entity is found.
// Returns a *NotFoundError when no AccountInfo entities are found.
func (aiq *AccountInfoQuery) Only(ctx context.Context) (*AccountInfo, error) {
	nodes, err := aiq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{accountinfo.Label}
	default:
		return nil, &NotSingularError{accountinfo.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (aiq *AccountInfoQuery) OnlyX(ctx context.Context) *AccountInfo {
	node, err := aiq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only AccountInfo ID in the query.
// Returns a *NotSingularError when more than one AccountInfo ID is found.
// Returns a *NotFoundError when no entities are found.
func (aiq *AccountInfoQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = aiq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{accountinfo.Label}
	default:
		err = &NotSingularError{accountinfo.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (aiq *AccountInfoQuery) OnlyIDX(ctx context.Context) int {
	id, err := aiq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of AccountInfos.
func (aiq *AccountInfoQuery) All(ctx context.Context) ([]*AccountInfo, error) {
	if err := aiq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return aiq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (aiq *AccountInfoQuery) AllX(ctx context.Context) []*AccountInfo {
	nodes, err := aiq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of AccountInfo IDs.
func (aiq *AccountInfoQuery) IDs(ctx context.Context) ([]int, error) {
	var ids []int
	if err := aiq.Select(accountinfo.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (aiq *AccountInfoQuery) IDsX(ctx context.Context) []int {
	ids, err := aiq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (aiq *AccountInfoQuery) Count(ctx context.Context) (int, error) {
	if err := aiq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return aiq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (aiq *AccountInfoQuery) CountX(ctx context.Context) int {
	count, err := aiq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (aiq *AccountInfoQuery) Exist(ctx context.Context) (bool, error) {
	if err := aiq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return aiq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (aiq *AccountInfoQuery) ExistX(ctx context.Context) bool {
	exist, err := aiq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the AccountInfoQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (aiq *AccountInfoQuery) Clone() *AccountInfoQuery {
	if aiq == nil {
		return nil
	}
	return &AccountInfoQuery{
		config:     aiq.config,
		limit:      aiq.limit,
		offset:     aiq.offset,
		order:      append([]OrderFunc{}, aiq.order...),
		predicates: append([]predicate.AccountInfo{}, aiq.predicates...),
		// clone intermediate query.
		sql:    aiq.sql.Clone(),
		path:   aiq.path,
		unique: aiq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Username string `json:"username,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.AccountInfo.Query().
//		GroupBy(accountinfo.FieldUsername).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
//
func (aiq *AccountInfoQuery) GroupBy(field string, fields ...string) *AccountInfoGroupBy {
	grbuild := &AccountInfoGroupBy{config: aiq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := aiq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return aiq.sqlQuery(ctx), nil
	}
	grbuild.label = accountinfo.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Username string `json:"username,omitempty"`
//	}
//
//	client.AccountInfo.Query().
//		Select(accountinfo.FieldUsername).
//		Scan(ctx, &v)
//
func (aiq *AccountInfoQuery) Select(fields ...string) *AccountInfoSelect {
	aiq.fields = append(aiq.fields, fields...)
	selbuild := &AccountInfoSelect{AccountInfoQuery: aiq}
	selbuild.label = accountinfo.Label
	selbuild.flds, selbuild.scan = &aiq.fields, selbuild.Scan
	return selbuild
}

func (aiq *AccountInfoQuery) prepareQuery(ctx context.Context) error {
	for _, f := range aiq.fields {
		if !accountinfo.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if aiq.path != nil {
		prev, err := aiq.path(ctx)
		if err != nil {
			return err
		}
		aiq.sql = prev
	}
	return nil
}

func (aiq *AccountInfoQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*AccountInfo, error) {
	var (
		nodes = []*AccountInfo{}
		_spec = aiq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*AccountInfo).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &AccountInfo{config: aiq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	_spec.Node.Schema = aiq.schemaConfig.AccountInfo
	ctx = internal.NewSchemaConfigContext(ctx, aiq.schemaConfig)
	if len(aiq.modifiers) > 0 {
		_spec.Modifiers = aiq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, aiq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (aiq *AccountInfoQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := aiq.querySpec()
	_spec.Node.Schema = aiq.schemaConfig.AccountInfo
	ctx = internal.NewSchemaConfigContext(ctx, aiq.schemaConfig)
	if len(aiq.modifiers) > 0 {
		_spec.Modifiers = aiq.modifiers
	}
	_spec.Node.Columns = aiq.fields
	if len(aiq.fields) > 0 {
		_spec.Unique = aiq.unique != nil && *aiq.unique
	}
	return sqlgraph.CountNodes(ctx, aiq.driver, _spec)
}

func (aiq *AccountInfoQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := aiq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (aiq *AccountInfoQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   accountinfo.Table,
			Columns: accountinfo.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: accountinfo.FieldID,
			},
		},
		From:   aiq.sql,
		Unique: true,
	}
	if unique := aiq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := aiq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, accountinfo.FieldID)
		for i := range fields {
			if fields[i] != accountinfo.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := aiq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := aiq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := aiq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := aiq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (aiq *AccountInfoQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(aiq.driver.Dialect())
	t1 := builder.Table(accountinfo.Table)
	columns := aiq.fields
	if len(columns) == 0 {
		columns = accountinfo.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if aiq.sql != nil {
		selector = aiq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if aiq.unique != nil && *aiq.unique {
		selector.Distinct()
	}
	t1.Schema(aiq.schemaConfig.AccountInfo)
	ctx = internal.NewSchemaConfigContext(ctx, aiq.schemaConfig)
	selector.WithContext(ctx)
	for _, m := range aiq.modifiers {
		m(selector)
	}
	for _, p := range aiq.predicates {
		p(selector)
	}
	for _, p := range aiq.order {
		p(selector)
	}
	if offset := aiq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := aiq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (aiq *AccountInfoQuery) Modify(modifiers ...func(s *sql.Selector)) *AccountInfoSelect {
	aiq.modifiers = append(aiq.modifiers, modifiers...)
	return aiq.Select()
}

// AccountInfoGroupBy is the group-by builder for AccountInfo entities.
type AccountInfoGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (aigb *AccountInfoGroupBy) Aggregate(fns ...AggregateFunc) *AccountInfoGroupBy {
	aigb.fns = append(aigb.fns, fns...)
	return aigb
}

// Scan applies the group-by query and scans the result into the given value.
func (aigb *AccountInfoGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := aigb.path(ctx)
	if err != nil {
		return err
	}
	aigb.sql = query
	return aigb.sqlScan(ctx, v)
}

func (aigb *AccountInfoGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range aigb.fields {
		if !accountinfo.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := aigb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := aigb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (aigb *AccountInfoGroupBy) sqlQuery() *sql.Selector {
	selector := aigb.sql.Select()
	aggregation := make([]string, 0, len(aigb.fns))
	for _, fn := range aigb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(aigb.fields)+len(aigb.fns))
		for _, f := range aigb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(aigb.fields...)...)
}

// AccountInfoSelect is the builder for selecting fields of AccountInfo entities.
type AccountInfoSelect struct {
	*AccountInfoQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (ais *AccountInfoSelect) Scan(ctx context.Context, v interface{}) error {
	if err := ais.prepareQuery(ctx); err != nil {
		return err
	}
	ais.sql = ais.AccountInfoQuery.sqlQuery(ctx)
	return ais.sqlScan(ctx, v)
}

func (ais *AccountInfoSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := ais.sql.Query()
	if err := ais.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (ais *AccountInfoSelect) Modify(modifiers ...func(s *sql.Selector)) *AccountInfoSelect {
	ais.modifiers = append(ais.modifiers, modifiers...)
	return ais
}
